# Database — MySQL connection string
DATABASE_URL="mysql://root:password@localhost:3306/fitflow"

# JWT Secret — production me strong random string use karo
JWT_SECRET="fitflow-super-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=4000
NODE_ENV=development

# CORS — frontend ka URL
CORS_ORIGIN="http://localhost:5173"
